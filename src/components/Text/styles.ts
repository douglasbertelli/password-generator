import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
	text: {
		width: '100%',
		padding: 12,
		borderRadius: 8,
		color: '#fff',
		textAlign: "center",
		fontSize: 24,
		backgroundColor: '#333'
	}

});

export default styles
